# Micromeritics AutoPore SMP 文件格式解析总结

> 适用仪器：AutoPore V 9600  
> 适用软件：MicroActive 2.03.00  
> 格式版本：MIC##&&FS 0200  
> 验证文件：已扩展到当前文件夹内 16 个有实际测试数据的 SMP 文件；`000-0055.SMP` 为空测试/未采集数据文件，读不到测量点属正常现象  
> 解析状态：核心数据块已完全解析，可正确读取所有测量数据和仪器参数；少数字段位置随文件大小变化，需动态定位

---

## 一、文件头（offset 0~52）

| 偏移量 | 长度 | 格式 | 含义 | 示例值 |
|--------|------|------|------|--------|
| 0~1 | 2字节 | 固定 | 文件前缀 | `5E 76` |
| 2~10 | 9字节 | ASCII | 格式签名 | `MIC##&&FS` |
| 11 | 1字节 | 固定 | 分隔符 | `00` |
| 12~15 | 4字节 | ASCII | 版本号 | `0200` |
| 16~19 | 4字节 | uint32 小端 | 创建时间（Unix时间戳） | 1742100406 |
| 20~23 | 4字节 | uint32 小端 | 修改时间 | 同上（未修改时相同） |
| 32~41 | 10字节 | ASCII | 根目录名 | `SUBSET101\x00` |
| 51~52 | 2字节 | uint16 小端 | 目录条目数 | 9 |
| 53起 | 每条10字节 | 结构体 | 目录条目 | 见下 |

**目录条目结构（10字节）：**
```
字节 0~1：数据类型编号（uint16 小端）
字节 2~5：该块在文件中的起始偏移量（uint32 小端）
字节 6~9：该块的字节大小（uint32 小端）
```

**重要机制：追加写入**  
修改样品描述等信息后，MicroActive 不覆盖原块，而是在文件末尾追加新块并更新目录表。解析时必须通过目录表动态查找各块位置，**不能硬编码偏移量**。

**重要说明：各块大小不固定**  
不同文件中同一 SUBSET 的 size 可能不同，多数字段相对偏移稳定，但少数字段也会发生块内偏移变化。因此关键块既要通过目录表动态定位，也要在块内做合理性扫描。

---

## 二、SUBSET626 — 样品描述（类型 0x0272）

### 2.1 文字字段（UTF-16-LE 变长字符串）

字段格式：`[长度(uint16)] [UTF-16字符] [结束符 \x00\x00]`

| 相对偏移 | 含义 |
|----------|------|
| +28 | 样品名称（为空时无内容） |
| +53 | 操作员（为空时无内容） |
| +84 | 提交人（为空时无内容） |

### 2.2 数值参数

| 相对偏移 | 格式 | 含义 | 示例值 |
|----------|------|------|--------|
| +185 | double | 样品质量（g） | 0.0399 |
| +193 | double | 体密度（g/mL，未勾选=1.0） | 1.0 |
| +201 | double | **⚠️ Bug值** = 真密度 + 样品质量 | 1.0399 |
| +209 | double | 粒子密度（g/mL，未勾选=1.0） | 1.0 |
| +5027 | double | 组件质量（g）= 穿透计+样品+汞 | 140.9067 |

> **Bug说明**：+201 处存储的值 = 真密度 + 样品质量（多文件验证）。  
> 正确还原真密度：`真密度 = data[+201] - data[+185]`  
> 注：该字段的值不影响任何已验证的计算结果。

### 2.3 低压抽真空参数

⚠️ **这些参数的绝对位置随 SUBSET626 大小变化，不能用固定偏移描述。**  
建议通过全文件搜索已知数值定位（如搜索 200.0 μmHg 对应的 double 字节）。

| 参数 | 含义 | 示例值 |
|------|------|--------|
| Sample类型 | 1=固体，2=粉末，3=其他 | 2 |
| Initially evacuate at | 抽真空速率（psia/min，仅Other模式写入文件） | 1.6 |
| Switch to fast at | 切换快速抽真空阈值（μmHg） | 200.0 |
| Evacuation target | 目标真空度（μmHg） | 50.0 |
| Continue evacuating for | 持续时间（min） | 5.0 |

---

## 三、SUBSET627 — 方法参数与测量数据（类型 0x0273）

### 3.1 方法参数区（块内相对偏移）

| 相对偏移 | 格式 | 含义 | 示例值 |
|----------|------|------|--------|
| +14~+55 | UTF-16 | 方法名称 | `Run Conditions` |
| +56 | 0x03 | 字段标志 | — |
| +59 | uint8 | LP平衡时间（s） | 10 |
| +73 | 0x03 | 字段标志 | — |
| +76 | uint8 | HP平衡时间（s） | 9 |
| +90 | 0x03 | 字段标志 | — |
| +91 | uint8 | HP分析模式（1=Time，2=待确认） | 1或2 |
| +138 | double | 前进接触角（°） | 130.0 |
| +146 | double | 后退接触角（°） | 130.0 |
| +154 | double | 汞表面张力（dynes/cm） | 485.0 |

> **Filling pressure 说明**：该值在部分文件的 SUBSET627+1026 处可读出，但不是所有文件有效（如 PDPAC37.SMP 该位置为无效值）。不建议用固定偏移读取，建议结合压力程序表判断。

### 3.2 实时汞密度记录

从块内约 +188 起，逐字节扫描找密度值（13.4~13.6 g/mL），每条 **17字节**：

```
字节 0~7 ：汞密度（double，g/mL）
字节 8   ：标志位（uint8，1=有效，171=结束）
字节 9~16：对应温度下的压力（double，psia）
```

> 注：第三字段（字节9~16）存储的是对应温度值（18.0、19.0、20.0...），不是测量时的实际压力。对照 Mercury Properties 里的温度-密度对照表，可还原出每条记录对应的温度。

**重要说明：**
- 只有高压分析初始阶段（约39个点）有实测汞密度
- 低压段和高压稳定段**没有实测密度**，密度-温度对照表存在软件配置文件里
- MicroActive 导出报告中大多数点的汞密度是软件推算的，非实测

### 3.3 压力程序表

从块内 +908 起，每条 **43字节**：

```
字节 0~7  ：结束压力（double，psia）
字节 8~15 ：压力增量（double，0=未设置）
字节 16~23：平衡时间（double，s）
字节 24~31：最大压入量（double，mL/g，0=未设置）
字节 32~39：扫描速率（double，0=未设置）
字节 40   ：标志位
字节 41~42：Points per Decade（uint16）
```

---

## 四、SUBSET628 — 穿透计参数（类型 0x0274）

### 4.1 型号字符串

块起始处为 UTF-16-LE 编码的穿透计型号，如：
```
07-0337 - (07)  5 Bulb, 0.392 Stem, Solid
```

### 4.2 数值参数（需块内动态定位）

穿透计参数不是所有文件都固定在 `SUBSET628+149` 起。常见文件使用 base=`149`，但 `QC-9.SMP` 的同一组数值整体后移 4 字节，使用 base=`153`。因此不要直接硬编码 `+149/+157/...`，而应在 SUBSET628 内扫描一组连续的 5 个 double：

| base相对偏移 | 格式 | 含义 | 合理范围 | 示例值 |
|----------|------|------|------|--------|
| base+0 | double | 穿透计常数（μL/pF） | 5~20 | 10.898 / 11.007 / 11.117 |
| base+8 | double | 穿透计质量（g） | 20~200 | 62.2847 / 63.8367 |
| base+16 | double | 球泡体积（mL） | 0.5~50 | 3.4389 / 5.9443 |
| base+24 | double | 茎管体积（mL） | 0.05~2 | 0.392 / 0.412 |
| base+32 | double | 最大头压（psia） | 0.5~20 | 4.45 / 4.68 |

推荐扫描逻辑：

```python
for base in range(120, subset628_size - 31):
    constant, mass, bulb, stem, max_head = read_5_doubles(base, step=8)
    if (
        5.0 <= constant <= 20.0
        and 20.0 <= mass <= 200.0
        and 0.5 <= bulb <= 50.0
        and 0.05 <= stem <= 2.0
        and 0.5 <= max_head <= 20.0
    ):
        break
```

已验证样例：

| 文件/类型 | base | 常数 | 质量 | 球泡 | 茎管 | 最大头压 |
|----------|------|------|------|------|------|----------|
| 1号等 5 mL bulb | 149 | 11.007/11.117 | 约63~65 | 约5.9~6.0 | 0.392 | 4.45 |
| 1-M5G2 / 2-TF700 等 3 mL bulb | 149 | 11.007/11.117 | 约62~63 | 约3.44 | 0.412 | 4.68 |
| QC-9 | 153 | 10.898 | 62.2847 | 3.4389 | 0.412 | 4.68 |

> 注意：部分 3 mL bulb 文件在旧固定偏移附近可看到 `0.007062499...` 之类的小数值，它不是茎管体积；正确茎管体积应由同一组 5-double 记录中的 `base+24` 给出。

> **穿透计常数的物理含义**：穿透计茎管是电容器（汞柱=导体，玻璃=绝缘层，外壁=电极）。电容每变化 1 pF，对应汞体积变化 11.007 μL（各穿透计不同）。这是仪器达到纳升精度的根本机制。

---

## 五、SUBSET630 — 核心测量数据（类型 0x0276）⭐

### 5.1 数据定位

块头部为 UTF-16 软件版本字符串。真实数据起点通常在块头部后约 317~321 字节处，但**不能用“找到前两个像压力的 double 就停止”的方式判断**。`1-M5G2-3-TF-3-0.85-1221.SMP` 和 `2-TF700-1-1129.SMP` 的 SUBSET630 头部中存在两个偶然像压力的 `2.0`，旧逻辑会误判为数据起点，只读出 2 个点。

可靠做法：逐字节扫描所有候选起点，按 24 字节步长统计连续合法记录数，选择**最长连续记录段**作为真实测量数据。

```python
# 推荐扫描逻辑
block_end = block_offset + block_size

def is_record(offset):
    if offset + 24 > block_end:
        return False
    pressure = read_double(offset)
    capacitance = read_double(offset + 8)
    counter = read_double(offset + 16)
    return (
        math.isfinite(pressure)
        and math.isfinite(capacitance)
        and math.isfinite(counter)
        and 0.1 < pressure < 60000
        and abs(capacitance) < 1e6
        and abs(counter) < 1e12
    )

best_start, best_count = None, 0
for candidate in range(block_offset, block_end - 23):
    count = 0
    i = candidate
    while is_record(i):
        count += 1
        i += 24
    if count > best_count:
        best_start, best_count = candidate, count

data_start = best_start
```

验证结果：上述两个文件从旧逻辑的 `2` 个点恢复为 `91` 个原始测量点；`QC-9.SMP` 也可稳定读取 `91` 个点。

### 5.2 每条记录（24字节）

```
字节 0~7 ：压力（double，psia）← 传感器原始读数，未做头压修正
字节 8~15：电容变化量（double，pF，负值，取绝对值使用）
字节 16~23：仪器内部计数值/时间计数（double，半周期内递增）
```

> 第三字段的具体单位未确认。它在同一采集段内递增，但 LP→HP 端口切换时会骤降，可用于识别切换点（通常为 index 32）。

### 5.3 核心换算公式

**累积孔体积（mL/g）：**
```
累积孔体积 = |电容(pF)| × 穿透计常数(μL/pF) ÷ 1000 ÷ 样品质量(g)
```

**孔径——Washburn 方程（nm）：**
```
D = -4 × γ × cosθ / P
γ：表面张力（N/m），485 dynes/cm = 0.485 N/m
θ：接触角，130°
P：压力（Pa），1 psia = 6894.757 Pa
```

### 5.4 MicroActive 的额外处理（SMP 存原始值）

| 处理项 | 说明 |
|--------|------|
| 汞柱头压修正 | `报告压力 = SMP原始压力 + 最大头压 × (1 - 已压入mL ÷ 茎管mL)`，差值约 3.2~3.9 psia |
| 切换噪点过滤 | index 32（压力约 23~24 psia）被过滤 |
| 退汞段单调不增约束 | 累积孔体积只减不增（过滤弹性回复信号） |
| 负增量显示为0 | "Show Neg. Int: No" 设置下退汞负值显示为0 |

---

## 六、SUBSET634 — 材料属性（类型 0x027A）

名义大小 92 字节，实际数据延伸至 +102（超出声明边界）。

| 相对偏移 | 格式 | 含义 | 示例值 |
|----------|------|------|--------|
| +26 | UTF-16 | 材料名称 | `Garnet` |
| +40 | double | BET比表面积（m²/g） | 200.0 |
| +48 | double | 未知（通常=0.0） | 0.0 |
| +50 | double | 体密度（g/mL，未勾选=1.0） | 1.0 |
| +58 | double | 真密度（g/mL，未勾选=1.0） | 1.0 |
| +66 | double | 粒子密度（g/mL，未勾选=1.0） | 1.0 |
| +76 | double | 导电形成因子 | 0.025 |
| +86 | double | 阈值压力（psia） | 0.10 |
| +94 | double | 线性压缩系数（1/psia）⚠️超出声明 | -2.74×10⁻⁷ |
| +102 | double | 二次压缩系数（1/psia²）⚠️超出声明 | 2.85×10⁻¹³ |

---

## 七、gap 区域 — LP端口原始缓冲（不在目录表中）

**起点固定：offset 29246**  
**终点：目录表中 SUBSET630 的起始 offset（不同文件可能为 30421 或 30423，不可硬编码）**

- 头部：UTF-16字符串 "analysis"
- 之后：逐字节扫描找第一个有效压力点，以 24 字节步长读取
- 内容：约 35~36 个数据点，格式与 SUBSET630 相同（24字节/条）
- 压力范围：LP低压端口（0~50 psia）的原始采集数据

---

## 八、SUBSET705 — 事件日志（类型 0x02C1）

UTF-16 字符串，记录：
```
Starting low pressure analysis
Finished low pressure analysis
Starting high pressure analysis
Finished high pressure analysis
```

---

## 九、SUBSET100 — 版本信息（类型 0x0064）

UTF-16字符串：软件版本 `V01.00`、仪器型号 `9600`、方法名 `Default`、状态 `Dirty`。

---

## 十、未完全解析的块

| Block | 含义 | 现状 |
|-------|------|------|
| SUBSET625 | 1字节标志位，含义未确认 | 不影响数据读取 |
| SUBSET629 | 报告显示选项（UTF-16文字） | 不含测量数据 |

---

## 十一、重要发现

### 1. 仪器测量的是电容，不是体积
穿透计茎管构成电容器，所有孔体积数据均由电容变化量换算。

### 2. MicroActive 的 Bug（SUBSET626+201）
存储值 = 真密度 + 样品质量（多文件验证）。正确读取：`真密度 = data[+201] - data[+185]`。

### 3. 汞密度大多是推算的
SMP 文件只有约39个点有实测汞密度，其余由软件推算。

### 4. MicroActive 对 HP 压力值做了头压修正
```
报告压力 = 传感器原始压力 + 最大头压 × (1 - 已压入体积mL ÷ 茎管体积mL)
```
差值约 3.2~3.9 psia，始终自动应用。

### 5. 追加写入机制
修改样品描述后文件末尾追加新块，解析时必须通过目录表动态定位。

### 6. SUBSET 编号是 Micromeritics 全公司统一体系
压汞用 6xx/7xx，BET 仪器用 3xx，共用文件头格式。

---

## 十二、速查表（目录表定位 + 块内扫描）

| 字段 | 位置 | 格式 |
|------|------|------|
| 样品质量 | SUBSET626+185 | double |
| 组件质量 | SUBSET626+5027 | double |
| 方法名称 | SUBSET627+14 | UTF-16 |
| LP平衡时间 | SUBSET627+59 | uint8 |
| HP平衡时间 | SUBSET627+76 | uint8 |
| HP分析模式 | SUBSET627+91 | uint8（1或2） |
| 前进接触角 | SUBSET627+138 | double |
| 后退接触角 | SUBSET627+146 | double |
| 表面张力 | SUBSET627+154 | double |
| 穿透计参数组 | SUBSET628 内扫描合理 5-double 记录 | base+0/+8/+16/+24/+32 |
| BET比表面积 | SUBSET634+40 | double |
| 体密度 | SUBSET634+50 | double |
| 真密度 | SUBSET634+58 | double |
| 粒子密度 | SUBSET634+66 | double |
| 导电因子 | SUBSET634+76 | double |
| 阈值压力 | SUBSET634+86 | double |
| 线性压缩系数 | SUBSET634+94 | double |
| 二次压缩系数 | SUBSET634+102 | double |
| 完整测量数据 | SUBSET630，逐字节扫描并选择最长连续合法记录段 | 24字节/条 |
| LP端口缓冲 | offset 29246，逐字节扫描起点 | 24字节/条 |
| 压力程序表 | SUBSET627+908起 | 43字节/条 |
| 汞密度记录 | SUBSET627约+188起，逐字节扫描 | 17字节/条：density+flag+pressure |

---

*本文档基于 AutoPore V 9600 + MicroActive 2.03.00（格式 MIC##&&FS 0200）。  
已使用当前文件夹内 16 个有实际测试数据的 SMP/XLS 文件交叉验证；`000-0055.SMP` 为空测试保存文件，不作为测量数据解析样本。*
